services:
  expense-tracker:
    build: .
    container_name: expense-tracker
    restart: unless-stopped
    env_file:
      - config.env
    environment:
      - TZ=Asia/Kolkata
      - CRON_SCHEDULE=0 9 * * *
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
